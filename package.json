{
  "name": "nelper-web",
  "version": "0.0.1",
  "description": "",
  "main": "webpack.config.js",
  "scripts": {
    "start-server": "iojs ./src/server.js",
    "build": "TARGET=build webpack",
    "watch": "TARGET=dev webpack-dev-server --devtool eval-source --progress --colors --hot --inline --history-api-fallback",
    "clean": "rm -rf ./build"
  },
  "engines": {
    "iojs": "^2.3.0"
  }
}
