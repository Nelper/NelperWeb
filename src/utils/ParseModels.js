import Parse from 'parse';

/**
 * Parse types
 */
export const Task = Parse.Object.extend({className: 'Task'});
export const TaskApplication = Parse.Object.extend({className: 'TaskApplication'});
export const UserPrivateData = Parse.Object.extend({className: 'UserPrivateData'});
export const Feedback = Parse.Object.extend({className: 'Feedback'});
