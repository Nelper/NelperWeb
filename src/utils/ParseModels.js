import {Parse} from 'parse';

import './ParsePatches';

/**
 * Parse types
 */
export const NelpTask = Parse.Object.extend({className: 'NelpTask'});
export const NelpTaskApplication = Parse.Object.extend({className: 'NelpTaskApplication'});
export const UserPrivateData = Parse.Object.extend({className: 'UserPrivateData'});
export const Feedback = Parse.Object.extend({className: 'Feedback'});
